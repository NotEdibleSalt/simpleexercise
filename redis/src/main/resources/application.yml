server:
  port: 8081
spring:
  redis:
    host: 127.0.0.1
    port: 6379
    password: password
  cache:
    type: redis
    redis:
      time-to-live: 200000
      use-key-prefix: true
      key-prefix: spring_cache_